import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.superapp-directory',
  appName: 'directory',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
